name: CI

on: 
  issues:
  

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ctx: ${{ toJson(github) }}
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: printenv

